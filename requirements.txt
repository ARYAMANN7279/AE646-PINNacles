numpy==1.26.4
scipy==1.13.0
matplotlib==3.8.4
torch==2.3.0
h5py==3.11.0
tqdm==4.66.4
pyyaml==6.0.1
wandb==0.17.3
requests==2.32.3

pytest

# Optional - only needed to rebuild the Stage 1 / Stage 2 .pptx decks
# (stage1/build_stage1_presentation.py, stage2/build_stage2_presentation.py).
# The reports and the Stage 3 deck compile with tectonic and need nothing from pip.
python-pptx==1.0.2
Pillow
